server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: form-demo

logging:
  level:
    com.formdemo: DEBUG
    root: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

